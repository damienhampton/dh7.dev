---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}
const { title, description } = Astro.props;
---
<!doctype html>
<html lang="en" class="antialiased">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,600;1,400&family=Space+Grotesk:wght@300;400;500;600&display=swap" rel="stylesheet" />
  </head>
  <body class="min-h-screen flex flex-col">

    <header class="bg-[#111110]">
      <nav class="max-w-3xl mx-auto px-6 py-4 flex items-center gap-8">
        <a href="/" class="font-[family-name:var(--font-sans)] font-600 text-[#FAFAF9] text-lg tracking-tight no-underline mr-auto hover:text-white transition-colors">DH7</a>
        <a href="/blog" class="font-[family-name:var(--font-sans)] text-sm font-400 text-[#A1A1A0] hover:text-[#FAFAF9] transition-colors no-underline">Writing</a>
        <a href="/integrations" class="font-[family-name:var(--font-sans)] text-sm font-400 text-[#A1A1A0] hover:text-[#FAFAF9] transition-colors no-underline">Integrations</a>
      </nav>
    </header>

    <main class="flex-1 max-w-3xl mx-auto w-full px-6 py-16">
      <slot />
    </main>

    <footer class="border-t border-[#E4E4E0] mt-auto">
      <div class="max-w-3xl mx-auto px-6 py-8 flex items-center justify-between">
        <span class="font-[family-name:var(--font-sans)] text-sm text-[#71717A]">Damien Hampton</span>
        <a href="mailto:damien@26brains.com" class="font-[family-name:var(--font-sans)] text-sm text-[#71717A] hover:text-[#1C1C1A] transition-colors no-underline">damien@26brains.com</a>
      </div>
    </footer>

  </body>
</html>
