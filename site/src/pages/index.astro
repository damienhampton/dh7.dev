---
import Base from '../layouts/Base.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
  .sort((a, b) => b.data.publishedAt.valueOf() - a.data.publishedAt.valueOf())
  .slice(0, 5);
---
<Base title="Damien Hampton — Software Engineer & Integration Developer">

  <section class="mb-16">
    <p class="text-xl leading-relaxed text-[#2A2520] max-w-lg">
      Hi, I'm Damien 👋. I'm a software engineer who builds integrations between the business systems rental and logistics companies depend on —
      <a href="/integrations/current-rms" class="text-[#A87820] hover:underline">Current RMS</a>,
      <a href="/integrations/pipedrive" class="text-[#A87820] hover:underline">Pipedrive</a>,
      <a href="/integrations/maxoptra" class="text-[#A87820] hover:underline">Maxoptra</a>,
      <a href="/integrations/deputy" class="text-[#A87820] hover:underline">Deputy</a>, and others.
    </p>
    <p class="mt-4 text-xl leading-relaxed text-[#2A2520] max-w-lg">
      If your team is copying data between systems, chasing updates manually, or working around gaps between tools — that's the problem I fix.
      <a href="/contact" class="text-[#A87820] hover:underline">Get in touch</a> and tell me what's broken.
    </p>
  </section>

  <section>
    <h2 class="font-[family-name:var(--font-sans)] text-xs font-500 uppercase tracking-widest text-[#7A7068] mb-6">📖 Recent writing</h2>
    <ul class="divide-y divide-[#E6DFD0]">
      {posts.map(post => (
        <li class="py-4 flex items-baseline justify-between gap-4">
          <a href={`/blog/${post.id}`} class="font-[family-name:var(--font-sans)] font-400 text-[#2A2520] hover:text-[#A87820] transition-colors no-underline">
            {post.data.title}
          </a>
          <time datetime={post.data.publishedAt.toISOString()} class="font-[family-name:var(--font-sans)] text-sm text-[#7A7068] whitespace-nowrap shrink-0">
            {post.data.publishedAt.toLocaleDateString('en-GB', { year: 'numeric', month: 'short' })}
          </time>
        </li>
      ))}
    </ul>
    <div class="mt-8">
      <a href="/blog" class="font-[family-name:var(--font-sans)] text-sm text-[#A87820] hover:underline">All posts →</a>
    </div>
  </section>

</Base>
